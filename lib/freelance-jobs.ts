import { LinkedInJob, JobSource, JobType } from '@/types'

/**
 * Trabajos freelance de múltiples plataformas
 * 
 * ⚠️ NOTA: Este archivo contiene trabajos de ejemplo/placeholder.
 * Las URLs no son reales y estas ofertas NO deben mostrarse al usuario.
 * 
 * Para obtener trabajos reales, usar el scraper en job-scraper.ts
 * que intenta hacer scraping real de Upwork, Turing y Freelancer.
 * 
 * Si el scraper falla (común debido a protección anti-bot), 
 * simplemente no se mostrarán ofertas de estas plataformas.
 * 
 * ✅ DESHABILITADO: Este archivo retorna un array vacío.
 * Solo se usarán trabajos reales obtenidos del scraper.
 */
export const freelanceJobs: LinkedInJob[] = []
